# Deterministic core
pandas>=2.0

# UI
streamlit>=1.30

# MCP server (agent access to the detector)
mcp>=1.0

# Web API (Intelligent Forecast dashboard backend)
fastapi>=0.110
uvicorn[standard]>=0.29

# Optional LLM briefs + agent answers (degrade gracefully with no key)
anthropic>=0.40

# Dev
pytest>=8.0
ruff>=0.5
black>=24.0
